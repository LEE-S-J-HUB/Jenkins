pipeline {
  agent any
  environment {
    AWS_PROFILE     = credentials('AWS_PROFILE')
  }
  stages {
    stage('Packer') {
      steps {
        sh """
        #!/bin/bash
        cd packer_build_test
        packer init .
        packer build .
        """
      }
    }
  }
}
