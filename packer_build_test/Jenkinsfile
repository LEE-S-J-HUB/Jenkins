pipeline {
  agent any

  stages {
    stage('Packer') {
      steps {
        sh """
        #!/bin/bash
        cd packer_build_test
        packer init .
        packer build aws-ubuntu.pkr.hcl
        """
      }
    }
  }
}
