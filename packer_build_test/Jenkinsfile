pipeline {
  agent any

  stages {
    stage('Packer - Build Docker Image') {
      steps {
        sh """
        #!/bin/bash
        cd packer_build_test
        packer init .
        packer build -force .
        """
      }
    }
  }
}
